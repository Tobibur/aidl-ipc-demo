<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission
        android:name="com.tobibur.aidl_server.MENU_SERVICE"
        android:protectionLevel="normal" />

    <application
        android:name=".AidlServerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIDLserver">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AIDLserver">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MenuService"
            android:exported="true"
            android:enabled="true"
            android:permission="com.tobibur.aidl_server.MENU_SERVICE">
            <intent-filter>
                <action android:name="com.tobibur.aidl_server.IMenuService" />
            </intent-filter>
        </service>

    </application>

</manifest>